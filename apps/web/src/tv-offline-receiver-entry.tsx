import React from 'react';
import ReactDOM from 'react-dom/client';
import { TvOfflineReceiver } from './TvOfflineReceiver';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TvOfflineReceiver />
  </React.StrictMode>
);
